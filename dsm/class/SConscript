# -*- python -*-

##
##  Import the cross-build environment.
##
Import('arm_env')

##
##  List the source files compiled into the libArmDsm.a library.
##
sources = Split("""
  Climet.cc
  Dpres.cc
  Neph.cc
  SampleTable.cc
  SerialTable.cc
  SyncRecord.cc
  RTL_DevIoctl.cc
  RTL_DevIoctlStore.cc
  RTL_DSMSensor.cc
  DSMSerialSensor.cc
""")

##
##  Build the libArmDsm.a library.
##
lib = arm_env.StaticLibrary('Dsm' , sources)
install = arm_env.Install('#lib_arm',lib)
Default(install)

