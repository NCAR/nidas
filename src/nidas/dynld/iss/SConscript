

Import('env')

sources = env.SharedObject (source = Split("""TiltSensor.cc"""),
                            CPPDEFINES = 'DEBUG')

headers = Split("""TiltSensor.h""")

env.Append (LIBNIDAS_DYNLD_SOURCES = [ env.File(f) for f in sources ])
env.Install('$PREFIX/include/nidas/dynld/iss', headers)

