#
# Simple installer.
#
# The mesa_fpga_file.bit file is generated By Mike Spowart.  It is
# generated from the anythingIO_260x.vhd file by a windows application.
#
from SCons.Script import Environment, Import

Import('env')
env: Environment

firmwares = env.Split("""
    mesa_fpga_file.bit
""")

# Only install the firmware when linux modules are enabled.
if env.LinuxModulesEnabled():
    env.Alias('install', env.Install('$PREFIX/firmware', firmwares))
