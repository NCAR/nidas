authorized_keys       - This contains the public keys of accounts
			allowed to log into the DSM.

id_dsa                - This is the DSM's private key.

id_dsa.pub            - This is root@DSM's public key.  You need to place
                        a copy of it in your $HOME/authorized_keys file.

id_dsa_dsm         - This is a private key for ssh-ing INTO root@dsm
                        Copy this file to your $HOME/.ssh folder.

known_hosts           - Ignore this file.  'ssh' generates this during
                        new login sessions.

server_config_example - Copy this file into your $HOME/.ssh folder and
                        update it's path to match your directory.
